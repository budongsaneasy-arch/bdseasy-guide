# 주간보고를 가이드·진단에 반영하는 방법

개발팀 주간보고를 받아서 **사용 가이드**와 **사용성 진단**에 반영하는 절차입니다.
매주 같은 방식으로 돌리기 위한 문서입니다.

---

## 1. 개발팀에 요청할 양식

아래를 그대로 복사해 개발팀에 전달하시면 됩니다.

> ### 주간보고 작성 요령
>
> 항목마다 **네 칸**만 채워 주세요. 한 줄이면 충분합니다.
>
> | 분류 | 어디 | 무엇이 달라졌나 | 배포 |
> | --- | --- | --- | --- |
> | 신규 | `매물 → 매물 접수` 사진 등록 | 사진을 끌어다 놓아도 올라갑니다 | 9/18 완료 |
> | 변경 | 계약 상세 → `계약서 관리` 탭 | 안내문자가 공동명의자 **전원**에게 갑니다 (전에는 대표 1인) | 9/18 완료 |
> | 버그수정 | `마케팅 → 일정 관리` | 월 이동하면 일정이 사라지던 것 수정 | 9/18 완료 |
> | 내부 | — | 메모리 누수 모니터링 | — |
>
> **네 칸의 뜻**
>
> - **분류** — `신규` / `변경` / `버그수정` / `내부` 중 하나
>   - 사용자 화면에 아무것도 안 보이면 `내부`입니다. 성능 개선, 리팩터링, 모니터링이 여기 해당합니다
> - **어디** — **제품 화면에 실제로 적혀 있는 글자 그대로** 써 주세요
>   - ○ `매물 → N 광고 관리` / `계약 상세 → 확인 설명서 탭`
>   - ✕ `광고 리스트 페이지` / `checklist 컴포넌트` ← 화면에서 이 글자를 찾을 수 없습니다
> - **무엇이 달라졌나** — **중개사 눈으로** 한 줄. 가능하면 `전에는 A → 이제 B`
>   - ○ `가계약 문자에 계약금 합계가 찍힙니다. 끄고 켤 수 있습니다`
>   - ✕ `총계약금 표시 및 표시여부 옵션 추가` ← 어디서 끄는지 알 수 없습니다
> - **배포** — `9/18 완료` / `9/29 예정` / `미정`
>   - **배포 전이면 가이드를 쓸 수 없습니다.** 날짜가 있어야 대기 목록에 넣습니다
>
> **공개하면 안 되는 항목**은 지금처럼 줄 앞에 `*` 를 붙여 주세요.
> 단, **사용자가 화면에서 직접 겪는 현상**이면 원인은 빼고 현상만 가이드에 쓸 수 있는지
> 한 줄 덧붙여 주시면 좋겠습니다 (예: 「현상만 안내 가능」).
>
> **화면이 바뀐 항목은 캡처 한 장**이 있으면 가장 좋습니다. 없어도 됩니다.

---

## 2. 받은 뒤에 하는 일

1. **분류** — 항목마다 `가이드 수정` / `진단 반영` / `확인만` / `해당 없음` / `배포 대기` 로 나눕니다
2. **확인** — 바뀐 화면을 실계정으로 직접 봅니다. 보고서 문구와 화면이 다르면 **화면을 따릅니다**
3. **가이드 수정** — 해당 문서를 고치고 필요하면 스크린샷을 다시 찍습니다
   ([스크린샷 규칙](./CLAUDE.md#스크린샷) · 실제 주소·성명은 촬영 전에 `○○` 로 바꿉니다)
4. **진단 갱신** — 새 문제는 항목으로 추가하고, **해결된 항목은 해결 표시**를 합니다
5. **회신** — 무엇을 했고 무엇이 막혔는지 표로 정리해 드립니다

확인이 필요한 항목은 **묻고 나서** 씁니다. 화면 이름·금액·법정 의무는 추측해서 쓰지 않습니다.

---

## 3. 분류 기준

| 분류 | 언제 | 결과물 |
| --- | --- | --- |
| **가이드 수정** | 중개사가 하는 일의 **순서나 화면이 달라졌을 때** | 문서 수정 + 커밋 |
| **진단 반영** | 고쳐야 할 것이 남아 있거나, 진단 항목이 **해결됐을 때** | 진단 아티팩트 갱신 |
| **확인만** | 버그 수정이라 문서는 그대로여도 되는 경우 | 화면만 확인, 기록 |
| **배포 대기** | 아직 배포 전 | 아래 대기 목록에 쌓아둠 |
| **해당 없음** | 내부 작업 | 없음 |

---

## 4. 배포 대기 목록

배포되면 가이드에 반영할 것들입니다. **반영하면 줄을 지웁니다.**

| 받은 주 | 항목 | 배포 | 반영할 문서 |
| --- | --- | --- | --- |
| 2026-09-19 | 렛츠 광고 재등록 기능 | 미정 (이번달 목표) | `howto/naver-ad.md` |
| 2026-09-19 | 고객 저장방식 전환 (원장중복 조치 포함) | 9/29 오전 7~8시 | `howto/public-records.md` 「이중으로 조회되는 문제」 |
| 2026-09-19 | 네이버 부동산 API 전환 | 미정 | `howto/naver-ad.md` 8단계 · `howto/n-monitoring.md` |

> **네이버 API 전환은 미리 알아야 합니다.** 동일매물 정보를 가져올 수 없게 되면
> `naver-ad.md` 8단계의 **순위·알림 설명이 통째로 바뀝니다.** 방향이 정해지면 알려 주세요.

---

## 5. 아직 문서가 없는 영역

주간보고에 자주 나오는데 가이드에 자리가 없는 것들입니다.

| 영역 | 이번 주 건수 | 메모 |
| --- | --- | --- |
| ~~이지허브 (이지 광고)~~ | 7건 | **2026-09-19 `howto/ez-hub.md` 로 만들었습니다** |
| **의뢰 접수·관리** | 1건 | `고객 → 의뢰 접수` · `의뢰 관리` · `허브 의뢰 관리` 문서가 없습니다 |

---

## 6. 주간 기록

| 주 | 항목 수 | 가이드 | 진단 | 커밋 |
| --- | --- | --- | --- | --- |
| 2026-09-19 | 18 | 기존 5개 수정 + `ez-hub.md` 신규 | N4·N5·06·07 보강, N7 신규 | `2171324` 외 |
