# 주간보고를 가이드·진단에 반영하는 방법

개발팀 주간보고를 받아서 **사용 가이드**와 **사용성 진단**에 반영하는 절차입니다.
매주 같은 방식으로 돌리기 위한 문서입니다.

---

## 1. 개발팀에 요청할 양식

아래를 그대로 복사해 개발팀에 전달하시면 됩니다.

> ### 주간보고 작성 요령
>
> 항목마다 **네 칸**만 채워 주세요. 한 줄이면 충분합니다.
>
> | 분류 | 어디 | 무엇이 달라졌나 | 배포 | 메모 |
> | --- | --- | --- | --- | --- |
> | 신규 | `매물 → 매물 접수` 사진 등록 | 사진을 끌어다 놓아도 올라갑니다 | 9/18 완료 | |
> | 변경 | 계약 상세 → `계약서 관리` 탭 | 안내문자가 공동명의자 **전원**에게 갑니다 (전에는 대표 1인) | 9/18 완료 | 공동명의 2인 이상 계약서에서만 보임 |
> | 변경 | 이지허브 매물 상세 | `매물요약표 인쇄` → `매물요약표 다운로드` 로 이름 변경 | 9/18 완료 | |
> | 버그수정 | `마케팅 → 일정 관리` | 월을 넘기면 일정이 사라지던 것 수정 | 9/18 완료 | 문의 #812 |
> | 내부 | — | 메모리 누수 모니터링 | — | |
>
> **네 칸의 뜻**
>
> - **분류** — `신규` / `변경` / `버그수정` / `내부` 중 하나
>   - 사용자 화면에 아무것도 안 보이면 `내부`입니다. 성능 개선, 리팩터링, 모니터링이 여기 해당합니다
> - **어디** — **제품 화면에 실제로 적혀 있는 글자 그대로** 써 주세요
>   - ○ `매물 → N 광고 관리` / `계약 상세 → 확인 설명서 탭`
>   - ✕ `광고 리스트 페이지` / `checklist 컴포넌트` ← 화면에서 이 글자를 찾을 수 없습니다
> - **무엇이 달라졌나** — **중개사 눈으로** 한 줄. 가능하면 `전에는 A → 이제 B`
>   - ○ `가계약 문자에 계약금 합계가 찍힙니다. 끄고 켤 수 있습니다`
>   - ✕ `총계약금 표시 및 표시여부 옵션 추가` ← 어디서 끄는지 알 수 없습니다
> - **배포** — `9/18 완료` / `9/29 예정` / `미정`
>   - **배포 전이면 가이드를 쓸 수 없습니다.** 날짜가 있어야 대기 목록에 넣습니다
> - **메모** — 있을 때만 적으시면 됩니다. 아래 셋 중 해당하는 것
>   - **재현 조건** — 특정 조건에서만 보이는 기능이면 한 줄. *예) 공동명의 2인 이상 계약서에서만 보임*
>     **이게 없으면 화면을 못 찾아 가이드를 못 씁니다.** 이번 주에 두 건이 그랬습니다
>   - **문의번호** — 고객 문의에서 나온 항목이면 번호만. 문의 글에 **사용자 본인의 말**이 있어서
>     「여기서 막히셨다면」을 쓸 때 가장 좋은 재료가 됩니다
>   - **바뀐 문구** — 이름·문구가 바뀌었으면 `전 → 후` 그대로. 그러면 문서 수정이 바로 끝납니다
>
> **공개하면 안 되는 항목**은 지금처럼 줄 앞에 `*` 를 붙여 주세요.
> 단, **사용자가 화면에서 직접 겪는 현상**이면 원인은 빼고 현상만 가이드에 쓸 수 있는지
> 한 줄 덧붙여 주시면 좋겠습니다 (예: 「현상만 안내 가능」).
>
> **버그수정은 「무엇이 잘못되고 있었나」를 적어 주세요.** 고친 사실만으로는 확인할 것이 없는지 알 수 없습니다.
> **가이드가 그 잘못된 동작을 설명하고 있었을 수 있어서** 그렇습니다.
>
> **없어지거나 이름이 바뀐 것도 한 줄 적어 주세요.** 가이드에는 그대로 남습니다.
> (`소유자 주소록`이 종료됐는데 메뉴 이름만 남아 있던 적이 있습니다)
>
> **아직 못 고쳤는데 사용자가 이미 겪고 있는 일**이 있으면 알려 주세요.
> 고치기 전이라도 **가이드로 먼저 막을 수 있습니다.**
>
> **화면이 바뀐 항목은 캡처 한 장**이 있으면 가장 좋습니다. 없어도 됩니다.

---

## 2. 받은 뒤에 하는 일

1. **소스 확인** — 프론트·백엔드 저장소를 `git fetch --tags` 하고, 지난 주간 기록 이후의 `release-prod/*` 태그 구간 커밋을 봅니다.
   항목마다 diff로 **방향(전에는 A → 이제 B)·재현 조건·바뀐 문구**를 확인하고, 태그에 없는 커밋은 `배포 대기`로 봅니다.
   보고서에 없는 화면 변경이 있으면 항목으로 추가합니다.
   저장소 주소·조회 방법·**옮기면 안 되는 것**은 [CLAUDE.md 규칙](./CLAUDE.md#주간보고는-소스-저장소-커밋으로-보완합니다)을 따릅니다
2. **분류** — 항목마다 `가이드 수정` / `진단 반영` / `확인만` / `해당 없음` / `배포 대기` 로 나눕니다
3. **확인** — 바뀐 화면을 실계정으로 직접 봅니다. 보고서·커밋과 화면이 다르면 **화면을 따릅니다**
4. **가이드 수정** — 해당 문서를 고치고 필요하면 스크린샷을 다시 찍습니다
   ([스크린샷 규칙](./CLAUDE.md#스크린샷) · 실제 주소·성명은 촬영 전에 `○○` 로 바꿉니다)
5. **진단 갱신** — 새 문제는 항목으로 추가하고, **해결된 항목은 해결 표시**를 합니다
6. **요청 전달** — 5절의 `전달 예정` 항목을 회신에 함께 실어 개발팀으로 보냅니다.
   **쌓아두기만 하면 전달되지 않습니다**
7. **회신** — 무엇을 했고 무엇이 막혔는지 표로 정리해 드립니다

확인이 필요한 항목은 **묻고 나서** 씁니다. 화면 이름·금액·법정 의무는 추측해서 쓰지 않습니다.

---

## 3. 분류 기준

| 분류 | 언제 | 결과물 |
| --- | --- | --- |
| **가이드 수정** | 중개사가 하는 일의 **순서나 화면이 달라졌을 때** | 문서 수정 + 커밋 |
| **진단 반영** | 고쳐야 할 것이 남아 있거나, 진단 항목이 **해결됐을 때** | 진단 아티팩트 갱신 |
| **확인만** | 버그 수정이라 문서는 그대로여도 되는 경우 | 화면만 확인, 기록 |
| **배포 대기** | 아직 배포 전 | 아래 대기 목록에 쌓아둠 |
| **해당 없음** | 내부 작업 | 없음 |

---

## 4. 배포 대기 목록

배포되면 가이드에 반영할 것들입니다. **반영하면 줄을 지웁니다.**

| 받은 주 | 항목 | 배포 | 반영할 문서 |
| --- | --- | --- | --- |
| 2026-09-19 | 렛츠 광고 재등록 기능 | 미정 (이번달 목표) | `howto/naver-ad.md` |
| 2026-09-19 | 고객 저장방식 전환 (원장중복 조치 포함) | 9/29 오전 7~8시 **(사전 공지 + 일정시간 서비스 중단)** | `howto/public-records.md` 「이중으로 조회되는 문제」 |
| 2026-09-19 | 네이버 부동산 API 전환 | 미정 | `howto/naver-ad.md` 8단계 · `howto/n-monitoring.md` |

> **네이버 API 전환은 미리 알아야 합니다.** 동일매물 정보를 가져올 수 없게 되면
> `naver-ad.md` 8단계의 **순위·알림 설명이 통째로 바뀝니다.** 방향이 정해지면 알려 주세요.

---

## 5. 개발팀에 요청한 것

가이드를 만들다 나온 **제품 쪽 요청**을 모읍니다. 주간보고와 함께 전달하고,
반영되면 **상태를 바꾸고 가이드에 반영**합니다.

| 올린 날 | 요청 | 상태 |
| --- | --- | --- |
| 2026-09-21 | 가이드 「틀린 점 알려주기」로 들어오면 **문의 폼을 미리 채워 주세요** | 전달 예정 |

### 2026-09-21 · 문의 폼 미리 채우기

**지금** — 가이드 문서 하단의 **틀린 점 알려주기**를 누르면 `bdseasy.com/board`(문의하기)로
갑니다. 그런데 **어느 문서에서 눌렀는지 화면이 모릅니다.** 문의 작성 화면이 빈 채로 열려서
중개사가 문서 이름을 직접 옮겨 적어야 합니다. 대개는 *"가이드에 틀린 게 있어요"* 로 끝나고,
받는 쪽에서 **어느 문서 어느 부분인지 다시 물어봐야** 합니다.

**요청** — 가이드가 링크에 출처를 실어 보낼 테니, 문의 화면에서 그 값을 읽어 미리 채워 주세요.

```
https://bdseasy.com/board/write?from=guide&doc=/howto/confirmation-form&title=확인설명서 작성하기 (주거용)
```

| 칸 | 채워질 값 |
| --- | --- |
| **제목** | `[가이드] 확인설명서 작성하기 (주거용)` |
| **문의타입** | `오류` 선택 |
| **내용** | `문서: https://guide.bdseasy.com/howto/confirmation-form` + 빈 줄 |

**왜** — 중개사가 덜 씁니다. 지금은 문서 이름을 옮겨 적는 한 단계 때문에 **그냥 넘어가는 분이 많습니다.**
문의타입에 이미 `오류`가 있어서, 가이드 오류가 따로 모이면 **어느 문서가 자주 틀리는지** 보입니다.

**가이드 쪽 작업** — `docs/.vitepress/config.mts` 의 `editLink.pattern` 을 함수로 바꿔
문서 경로를 쿼리로 붙이면 됩니다. **제품 쪽이 준비되면 같이 바꿉니다.**

---

## 6. 아직 문서가 없는 영역

주간보고에 자주 나오는데 가이드에 자리가 없는 것들입니다.

| 영역 | 이번 주 건수 | 메모 |
| --- | --- | --- |
| ~~이지허브 (이지 광고)~~ | 7건 | **2026-09-19 `howto/ez-hub.md` 로 만들었습니다** |
| ~~의뢰 접수·관리~~ | 1건 | **2026-09-19 `howto/request.md` 로 만들었습니다** |

---

## 7. 주간 기록

| 주 | 항목 수 | 가이드 | 진단 | 커밋 |
| --- | --- | --- | --- | --- |
| 2026-09-19 | 18 | 기존 7개 수정 + `ez-hub.md` · `request.md` 신규 | N4·N5·06·07 보강, N7·N8·N9 신규 | `2171324`~`7be1070` |

**2026-09-19 개발팀 회신으로 바로잡은 것** — 물어보길 잘한 두 건입니다.

| 무엇 | 제가 쓴 것 | 실제 |
| --- | --- | --- |
| 안내문자 공동명의 | 전원에게 함께 보낸다 | **반대** — 단체문자로 나가던 것을 **고른 한 명에게만** 가도록 고친 것 |
| 중개사무소 홈페이지 | 플러스 요금제 전용 | **요금제와 무관하게 전부 제공** (강의자료가 옛 정보) |

보고서 한 줄만 보고 방향을 짐작하면 이렇게 반대로 씁니다.
**「무엇이 달라졌나」를 `전에는 A → 이제 B` 로 받아야 하는 이유입니다.**
